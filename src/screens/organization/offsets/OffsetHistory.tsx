import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const OffsetHistory = () => {
  return (
    <View>
      <Text>OffsetHistory</Text>
    </View>
  );
};

export default OffsetHistory;

const styles = StyleSheet.create({});
