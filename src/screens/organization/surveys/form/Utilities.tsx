import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Utilities = ({monthName, year}: any) => {
  return (
    <View>
      <Text>Utilities</Text>
    </View>
  );
};

export default Utilities;

const styles = StyleSheet.create({});
