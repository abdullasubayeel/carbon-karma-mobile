import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const EmpDashboard = () => {
  return (
    <View>
      <Text>EmpDashboard</Text>
    </View>
  );
};

export default EmpDashboard;

const styles = StyleSheet.create({});
